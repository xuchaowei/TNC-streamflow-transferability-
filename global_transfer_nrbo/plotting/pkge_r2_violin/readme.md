# +

#### 1.

- ,
-  Type ,, `grid::str_wrap()`,

#### 2. -1

-  create_violin_plot ,,:
1. `rasterGrob()`,`colorRampPalette(bg_colors)(256)` bg_colors `width``height``interpolate` TRUE ,
2. `annotation_custom()` xmin, xmax, ymin, ymax , -Inf  Inf ,,
3. `geom_violin()`, trim  FALSE `stat_summary()`
4. `geom_signif()`, p
5. `element_textbox_simple()`,

#### 3. -2

- `params`,,, (age_group) (title)y (y_limits)y (y_breaks) (p_position) (fill_color)  (bg_colors)
- `lapply()``params`,`create_violin_plot()``plots`
- `wrap_plots()` (`plots`)

######

### :

,,:

 **Ms.Tt (Ttian6688)** __

 **MsTt** __
